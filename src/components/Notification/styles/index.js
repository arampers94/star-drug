import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  close: {
    color: 'white',
    padding: '4px'
  },
  goToCartButton: {
    color: '#0ab6f0'
  },
})

export default useStyles;